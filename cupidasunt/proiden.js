// Example with an array
let query = [1, 2, 3, 4, 5];
let m = query.length; // m will be 5 (length of the array)

// Example with a string
query = "Hello";
m = query.length; // m will be 5 (length of the string)
